# Template for terraform.tfvars. Copy this file to terraform.tfvars and
# replace the placeholders with real values.

db_password    = "ChangeMe123!"
azure_base_url = "https://eventapp-func-zhw36q.azurewebsites.net/api"

# MUST be the exact same value in terraform/azure/terraform.tfvars too -
# generate one with: python3 -c "import secrets; print(secrets.token_hex(32))"
jwt_secret = "GENERATE-YOUR-OWN-64-CHAR-HEX-STRING-AND-USE-SAME-VALUE-IN-BOTH-CLOUDS"
